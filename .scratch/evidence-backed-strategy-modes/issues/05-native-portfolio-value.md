# 05 — Simulate native buff timelines and parameter value

**What to build:** Compare complete before-and-after Code portfolios using native buff coexistence, timing, probability, offense, and defense mechanics instead of displayed combat power.

**Blocked by:** 02 — Expand authoritative strategy evidence contracts.

**Status:** complete

> Closure supersedes the earlier chronological “re-review pending” notes below; they remain only as an audit trail of each review-fix iteration.

- [x] Allow effects combine only inside their matching native groups and simultaneous windows, respecting any positive cumulative limit.
- [x] HigherValue effects replace only under the native value and remaining-duration rule.
- [x] A displaced weaker group is removed and is not assumed to resume later.
- [x] Candidate and held effects are evaluated as one retained-portfolio timeline with authoritative triggers and durations.
- [x] Native attack, defense, HP, taken-damage, resistance, element, critical, continuous-attack, and damage-modifier relationships are used only when their required inputs are known.
- [x] Critical probability clips marginal value at the native guaranteed-critical threshold.
- [x] Continuous-attack probability follows its finite decreasing-probability ladder and live maximum count rather than the critical cap.
- [x] Same-recipient defense compares exact relative effective HP; different recipients compare rear-row coverage, weakest covered rear gain, then aggregate gain.
- [x] Boss-Value Reference keeps repeatable and delayed effects relevant when they can trigger during Boss combat.
- [x] Missing enemy or timing evidence yields Reachable-Unquantified or candidate-local failure instead of an invented DPS/EHP scalar.
- [x] Displayed combat power cannot alter ordering in characterization tests.
- [x] Mandatory two-axis checkpoint review passed independently (Spec PASS; Standards PASS).

**Implementation evidence:** Fresh Docker/RO inspection of `Project.dll` SHA-256 `53806a5b4dec186357e2fe8ba5b8a72e4f85674be9231479e207e500e2bd1300` and `GameAssembly.dll` SHA-256 `573fa800171b8b37800cb4425b918351ec84a340bca9a46c32249d7af965c1fb` anchors Allow grouping/cumulative limits to `BuffGroup.AddBuff`/`GetSumValue`/`GetMaxLimit`, HigherValue replacement to `BuffController.CheckCoexistenceHigherValue` plus `BuffGroup.GetHighestValueBuff`/`IsLongerRemainTime`/`RemoveAllBuff`, guaranteed critical to `CriticalRate.CalculateCritical` at 999, and the finite decreasing continuous ladder to `UnitAttackContinuous.IsSuccessAttackContinuous` with live `AttackContinuousCntMax`. Current Code-offer party models expose no authoritative live continuous maximum, so that dependent value remains explicit unknown rather than using critical semantics. Production now evaluates each candidate against the complete retained before/after native timeline, including the exact removed Code at capacity; a strong isolated offer cannot displace a stronger retained effect. Exact native `Calculate_Unit` inputs feed attack/HP/defense comparisons, and defense ordering uses recipient identity, rear coverage, weakest rear relative EHP gain, then aggregate same-unit gain without a cross-mechanism scalar. Fresh lifecycle tracing also proves Event HP is applied by `NetherEventResultModel` -> `NetherPartyModel.UpdateCharacterStatuses` from `NetherUpdateEventResponseEntity.t_nether_characters`, battle HP comes from `NetherClearBattleResponseEntity.t_nether_characters`, and future damage remains live/random in `UnitDamageCalculator`; the current offer lifecycle cannot positively prove an HP/defence Code repairs an existing survival deficit. Production preserves the proven deficit and rejects only that dependent candidate with the exact component-local reason rather than fabricating repair. Controller, battle-result, and recovered-offer paths consume the same complete mutations; displayed power, zero/nonpositive value, missing evidence, and unquantified magnitude cannot prove selection. The Docker affected suite passed 175/175 and the full suite passed 1016/1016. Two clean RO-source/RO-game Release builds were byte-identical at SHA-256 `c9f7efb19284a8f34358f9e1c158daadf911410f8586da258971bb8336c82b74`, 1,447,424 bytes, with 0 warnings/0 errors. Final-review closure adds behavior-sensitive production evidence for delayed/repeatable ParameterBuff windows: StartBattle is one-shot, BattleSituationDuration uses its exact millisecond period, AbilityEffectParameterBuff's Duration end-situation supplies the applied lifetime, and the Boss horizon resolves through the map-floor battle relation to `MNetherBattleStages.time_limit`. Unsupported live `BuffTargetFilter` relationships are typed candidate-local unknown rather than a fabricated no-recipient result. The affected Docker/game-RO suite passed 98/98 and the full suite passed 1018/1018. Two clean RO-source/RO-game Release builds were byte-identical at SHA-256 `966c30ca3984f8c602df5195f802c3c2b63e864a9ad0dbd3717f18b564053583`, 1,457,152 bytes, with 0 warnings/0 errors. Mandatory two-axis re-review remains pending.

**Latest review-fix evidence:** Critical, continuous-attack, and defense comparisons now receive the complete common-plus-side before/after portfolios, so native HigherValue suppression, caps, and saturation apply to the actual retained set. Equal or saturated exact results remain known zero delta rather than becoming unavailable or fabricating a preferred side. Target-filter evaluation uses the selected `BuffParameterByType`, requires exact parameter capture, preserves unknown native flag bits, and rejects unsupported combinations only in the dependent mechanic while exact siblings remain usable. The focused public popup/assembler/mechanism suite passed 58/58, the broader affected suite passed 221/221, and the full Docker/game-RO suite passed 1036/1036. Two builds derived from one immutable RO source snapshot and the same RO game mount were byte-identical at SHA-256 `f0fe47392d1b6b2920a6cd3dfc9310998650942b17fa399ddf47cb7e203848c4`, 1,478,144 bytes, with 0 warnings/0 errors; artifacts are `release/strategy-batch-04-06-converged2-20260816-a/Release/net6.0/AutoNether.dll` and the matching `...-b/...` build. Mandatory two-axis re-review remains pending.

**Latest complete-defense/cross-domain review-fix evidence:** Fresh Cpp2IL for `UnitDamageCalculator.DamageModifier` proves `TakenDamageDown` (`BuffType` 52) is subtracted from the incoming-damage modifier, while `ParameterCalculator.Calculate_Unit` remains the exact MaxHP/Defence authority. Production defense comparison now projects the after portfolio as deltas from the authoritative current calculations and compares complete MaxHP + Defence + TakenDamageDown EHP per recipient; resistance remains its separate native domain and is never hidden by that special. The four-case RED showed three held defensive domains falsely replaced and a MaxHP-to-Defence projection of 13,750 instead of 12,500; GREEN is 4/4. Critical/continuous versus ordinary offense remains explicitly incomparable in both directions unless a same-domain native comparison proves improvement. The focused assembler suite passed 47/47, the affected suite 231/231, and the full suite 1046/1046 in Docker with the game mounted RO. Compile-verification builds were byte-identical at SHA-256 `c6d893fc9021a9875f06c2d2514bd0d94d77493a1fb91d27fbeb9545a5f9cee5`, with final staged artifact identity deferred to re-review. Mandatory two-axis re-review remains pending.

**Offer-comparator closure evidence:** Offer-to-offer comparison now unions the complete native-special dimension set. When only one same-tier offer carries Critical or Continuous probability evidence and the other carries ordinary Attack, the missing dimension is explicit incomparability and ordering falls through only to deterministic Code ID; reversing IDs cannot make Attack win by treating the absent special as zero. When both offers share the exact same special domain, native magnitude remains comparable. The production RED failed the lower-special-ID Critical and Continuous cases (2 failures across 8 cases); GREEN passed 8/8, with affected/full results 239/239 and 1054/1054. Mandatory two-axis re-review remains pending.

**Per-recipient special-vector closure evidence:** Fresh `CriticalRate.CalculateCritical` and `UnitAttackContinuous` evidence under the current hashes supports per-character probability outcomes, not an aggregate scalar. Production now preserves Character ID, Forward/Back/Assist position, native special domain, and direction for every Critical/Continuous before/after row. A Back gain may trade a Forward loss by the approved tier order; the reverse keeps the held portfolio; same-tier mixed recipients remain Reachable-Unquantified rather than netting. The production RED failed four mixed-recipient direction cases in the 7-failure/12-case run; GREEN passed 12/12, the expanded assembler suite 73/73 plus 2/2 same-tier controls, affected 259/259, and full 1074/1074. Mandatory two-axis re-review remains pending.

**Coexistence residual-vector evidence:** Complete native timelines now emit their actual nonzero `RecipientCharacterId` + metric marginals into the same typed tier resolver used by native specials and recipient-scoped mechanisms. Suppressed or saturated zero rows contribute no direction and cannot lend their declared mechanic's tier to another surviving row. The focused contamination RED/controls and final regression counts are recorded in Ticket 04; mandatory two-axis re-review remains pending.

**Offer-ranking residual-tier evidence:** Normal Equipment offer comparison now returns the decisive Combat Tier from the simulated complete portfolio's actual nonzero residual recipient rows rather than the offered ability's pre-simulation All/row declaration. The production HigherValue suppression RED, reverse controls, and final 85/85 assembler plus 1084/1084 full-suite results are recorded in Ticket 04. Mandatory two-axis re-review remains pending.

**Final checkpoint review closure:** Independent staged-snapshot review passed both axes: Spec PASS and Standards PASS. The reviewer materialized the exact index with `git checkout-index`, mounted the game read-only, passed the full Release suite 1084/1084, and produced two byte-identical 0-warning/0-error builds at SHA-256 `d771624e2e9fce8a90133174cc84ef5f988d1d99986bb69d6ef31834837437f2`. Fresh authorities remained `Project.dll` `53806a5b4dec186357e2fe8ba5b8a72e4f85674be9231479e207e500e2bd1300`, `GameAssembly.dll` `573fa800171b8b37800cb4425b918351ec84a340bca9a46c32249d7af965c1fb`, and metadata `ac0c6d43ca487456a5de68a5d357f634fedd9fa0a87d80d5b6545360fb133ea5`. Working-tree and staged diff checks passed; no deployment was performed.
