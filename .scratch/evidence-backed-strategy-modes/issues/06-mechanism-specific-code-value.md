# 06 — Value crest, charge, stack, erosion, and Force Chain mechanics

**What to build:** Add evidence-backed marginal value for Code mechanics that cannot be reduced to ordinary parameter percentages, while keeping qualitative priorities explicit and bounded.

**Blocked by:** 02 — Expand authoritative strategy evidence contracts; 03 — Project route-horizon safety and erosion recoverability; 04 — Enforce Code-family integrity and hard eligibility; 05 — Simulate native buff timelines and parameter value.

**Status:** complete

> Closure supersedes the earlier chronological “re-review pending” notes below; they remain only as an audit trail of each review-fix iteration.

- [x] Crest Payoff Reachability requires an authoritative grant-provider and consumer path for the actual recipients.
- [x] ManaType proves crest identity but never proves self-grant or trigger cadence by itself.
- [x] Shared Mana Injection uses the shared 0–10 pool, scope match count, modifiers, and remaining capacity.
- [x] Initial Skill Charge clips independently at each recipient's ready threshold.
- [x] Skill-Charge Efficiency retains recurring value after gauge reset and is not erased by a full first cast.
- [x] Stack-linked value uses a proven per-character timeline or guaranteed lower bound and never assumes maximum stacks.
- [x] Erosion-linked value uses projected erosion at each confirmed combat rather than maximum description text.
- [x] Immediate category-threshold crossings add or remove only their current authoritative effect; distance to a future threshold has no option value.
- [x] Back-row numerical Force Chain payoff receives its approved high qualitative priority from actual completion-message reachability without invented cadence.
- [x] Front-row Force Chain payoff remains a fallback, and any crest-granting Force Chain effect still passes hard compatibility.
- [x] Charge, stack, and probability saturation can reduce marginal value to zero without banning the entire mechanic family.
- [x] Tests distinguish proven quantitative value, approved qualitative priority, Reachable-Unquantified value, and missing trigger evidence.
- [x] Mandatory two-axis checkpoint review passed independently (Spec PASS; Standards PASS).

**Implementation evidence:** Fresh Docker/RO inspection of `Project.dll` SHA-256 `53806a5b4dec186357e2fe8ba5b8a72e4f85674be9231479e207e500e2bd1300`, `GameAssembly.dll` SHA-256 `573fa800171b8b37800cb4425b918351ec84a340bca9a46c32249d7af965c1fb`, and `global-metadata.dat` SHA-256 `ac0c6d43ca487456a5de68a5d357f634fedd9fa0a87d80d5b6545360fb133ea5` anchors shared mana to `ManaGemEnergy.Min`/`Max`/`ChargeEnergy`/`ModifyChargeAmount` and `AbilityChargeMana.ExecuteInternal`, initial/recurring charge to `AbilitySkillCharge.ExecuteInternal`, `ActionSkillCharge.AddChargeCount`, and `ResetChargeCount`, stack identity to `AbilityStackLinkedBuff.GetStackCount`, erosion interpolation to `AbilityErosionLinkedBuff.OnChangedHpLinkedIncrease`/`Decrease`, category thresholds to `NetherCodeCategoryTypeExtensions.GetCategoryRawCount`/`GetCategoryCount`, and Force Chain reachability to `BattleTriggerActivateForceChain.Initialize` plus `ForceChainFinishedMessage`. Fresh Cpp2IL proves ChargeManaRate applies only when the summed modifier is positive before the shared 0–10 clamp, `GetStackCount` is instantaneous and cannot invent a future Boss timeline, and erosion interpolation uses one-argument `Math.Round(double)` (ToEven). Production uses one typed mechanism classifier/dispatcher and complete before/after mechanism portfolios: exact confirmed-combat erosion and reachable Force Chain remain usable; shared mana, charge, recurring charge, stack, crest payoff, and category crossings become candidate-local Reachable-Unquantified when the offer lifecycle lacks their required live pool, per-recipient timeline, provider/consumer graph, or decoded threshold effect. This never contaminates an independently exact ordinary candidate. The same current lifecycle proves that post-Event and post-battle HP arrive only in server response character rows and future battle damage is live/random; without an authoritative before/after survival projection, a Code Offer cannot fabricate the survival-repair tier. Equipment then orders approved Back Force Chain after survival is satisfied, rear/full offense, rear/full nonessential defense, and front fallback, comparing magnitudes only within commensurate native metric/recipient groups. The Docker affected suite passed 175/175 and the full suite passed 1016/1016. Two clean RO-source/RO-game Release builds were byte-identical at SHA-256 `c9f7efb19284a8f34358f9e1c158daadf911410f8586da258971bb8336c82b74`, 1,447,424 bytes, with 0 warnings/0 errors. Final-review dispatch audit keeps mechanism classification, retained-portfolio construction, and typed tier/value consumption on the single authoritative classifier; unsupported dependent target/timeline relationships remain smallest-component unknown and do not contaminate exact siblings. The affected Docker/game-RO suite passed 98/98 and the full suite passed 1018/1018. Two clean RO-source/RO-game Release builds were byte-identical at SHA-256 `966c30ca3984f8c602df5195f802c3c2b63e864a9ad0dbd3717f18b564053583`, 1,457,152 bytes, with 0 warnings/0 errors. Mandatory two-axis re-review remains pending.

**Latest review-fix evidence:** The single typed dispatcher now preserves the exact mechanism quantity domain through classification, portfolio construction, and tier comparison. Erosion-linked values carry native `BuffType` plus parameter-reference identity, so equal scalar magnitudes from different effect/stat domains cannot compensate or prove replacement; same-domain values remain comparable. The Min linked Buff parameter supplies the exact erosion target identity/filter and Max supplies the interpolation endpoint; unknown or unsupported target evidence stays candidate-local. The focused public popup/assembler/mechanism suite passed 58/58, the broader affected suite passed 221/221, and the full Docker/game-RO suite passed 1036/1036. Two builds derived from one immutable RO source snapshot and the same RO game mount were byte-identical at SHA-256 `f0fe47392d1b6b2920a6cd3dfc9310998650942b17fa399ddf47cb7e203848c4`, 1,478,144 bytes, with 0 warnings/0 errors; artifacts are `release/strategy-batch-04-06-converged2-20260816-a/Release/net6.0/AutoNether.dll` and the matching `...-b/...` build. Mandatory two-axis re-review remains pending.

**Latest shared-Mana reachability review-fix evidence:** Fresh `Project.dll` evidence shows `AbilityChargeMana.Initialize` receives the exact `AbilityTargetBase`; `AbilityTargetGroupBase.IsMatch`/`IsMatchFilter` resolve element and party-position relationships before `ExecuteInternal` can mutate the shared `ManaEnergyOrbsController`. Production therefore proves at least one exact trigger recipient before applying the party-global resource rule: zero exact matches are quantified zero/ineligible, unsupported union/search/provider evidence is candidate-local Missing, and an exact match advances only to the live pool/modifier Reachable-Unquantified gate with its recipient count. The production RED failed 0/2 under the old generic shared-Mana result; GREEN is 2/2. Together with the cross-tier/cross-domain and complete-defense slices, the focused assembler suite passed 47/47, the affected suite 231/231, and the full Docker/game-RO suite 1046/1046. Two clean compile-verification builds were byte-identical at SHA-256 `c6d893fc9021a9875f06c2d2514bd0d94d77493a1fb91d27fbeb9545a5f9cee5`, 1,482,752 bytes, with 0 warnings/0 errors; final checkpoint artifact identity remains deferred to the staged immutable snapshot. Mandatory two-axis re-review remains pending.

**Typed-loss closure evidence:** Fresh `AbilityErosionLinkedBuff.Param` evidence preserves the exact BuffType, Min/Max threshold parameters, and target filter, while `BattleTriggerActivateForceChain` completes from `ForceChainFinishedMessage`. Production keeps the resulting erosion quantity loss and Back Force Chain qualitative gain as distinct typed signs and applies the approved Combat Tier ordering without a scalar. Back Force Chain over front erosion selects; front erosion over Back Force Chain keeps. The focused RED/GREEN and final suite counts are recorded in Tickets 04 and 05; mandatory two-axis re-review remains pending.

**Erosion-recipient tier closure evidence:** Fresh `AbilityErosionLinkedBuff.Param` plus current `Project.BuffType` and parameter-reference subtype evidence now drives a single typed classification path through mechanism capture, target matching, portfolio construction, and Combat Tier evaluation. Exact Back/All Attack, Damage, Critical, Continuous, Defence, MaxHP, TakenDamage, resistance, and supported element domains receive their recipient-aware offense/defense tier; Forward-only targets are fallback; unknown BuffType or mismatched parameter-reference domains fail only the dependent candidate. The production RED observed rear Attack incorrectly tiered `None` and an unknown BuffType incorrectly quantified; GREEN passed 12/12, assembler 73/73, affected 259/259, and full 1074/1074. Mandatory two-axis re-review remains pending.

**Per-recipient erosion quantity evidence:** Fresh `AbilityErosionLinkedBuff.Param` and `IAbilityPassiveBuff.GetValue`/`IsMatchBuff`/`TryGetTargetFilter` evidence proves one exact typed value per matching target. Erosion-linked portfolio evidence now retains Character ID, Forward/Back/Assist position, combat metric, BuffType, and parameter-reference domain instead of multiplying one value by a recipient count. Held Back Attack +10 to offered Forward Attack +100 therefore keeps; Forward-to-Back may select; same-recipient same-domain magnitude remains comparable; unknown domains stay candidate-local. The production RED failed the Back-to-Forward case while both controls passed (1 failure/3); GREEN passed all 3 as part of the 6/6 focused run. One `NetherEquipmentCombatTierClassifier` now classifies ordinary metrics, native specials, and qualitative mechanisms. Assembler/affected/full passed 81/81, 265/265, and 1080/1080; mandatory two-axis re-review remains pending.

**Unified decisive-tier regression evidence:** The same centralized classifier now supplies the effective result tier selected from surviving nonzero ordinary, recipient-scoped mechanism, special, and qualitative marginals; a suppressed ordinary All row cannot bypass the per-recipient mechanism/erosion rules by lending its declared tier to another residual. Candidate-local unknown and earlier erosion/Force-Chain recipient controls remain green inside the 269/269 affected and 1084/1084 full Docker/game-RO runs. Mandatory two-axis re-review remains pending.

**Final checkpoint review closure:** Independent staged-snapshot review passed both axes: Spec PASS and Standards PASS. The reviewer materialized the exact index with `git checkout-index`, mounted the game read-only, passed the full Release suite 1084/1084, and produced two byte-identical 0-warning/0-error builds at SHA-256 `d771624e2e9fce8a90133174cc84ef5f988d1d99986bb69d6ef31834837437f2`. Fresh authorities remained `Project.dll` `53806a5b4dec186357e2fe8ba5b8a72e4f85674be9231479e207e500e2bd1300`, `GameAssembly.dll` `573fa800171b8b37800cb4425b918351ec84a340bca9a46c32249d7af965c1fb`, and metadata `ac0c6d43ca487456a5de68a5d357f634fedd9fa0a87d80d5b6545360fb133ea5`. Working-tree and staged diff checks passed; no deployment was performed.
