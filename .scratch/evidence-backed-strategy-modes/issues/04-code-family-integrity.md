# 04 — Enforce Code-family integrity and hard eligibility

**What to build:** Reject or repair Codes that violate family opposition, crest compatibility, authoritative future research-rate, or Risk safety rules before either strategy mode considers their payoff.

**Blocked by:** 02 — Expand authoritative strategy evidence contracts; 03 — Project route-horizon safety and erosion recoverability.

**Status:** complete

> Closure supersedes the earlier chronological “re-review pending” notes below; they remain only as an audit trail of each review-fix iteration.

- [x] Raw Family Count and Effective Family Count use distinct positive-amount Codes and authoritative opposed-family subtraction.
- [x] Automation never intentionally adds Rush with Impact or Safe with Risk.
- [x] Existing contamination retains the configured Research side or the greater Actual Combat Value Equipment side and removes the weaker side first at replacement.
- [x] Every row-wide uniform crest grant is rejected for a Mixed-Crest Row.
- [x] Crossing effective Rush or Impact count five is rejected unless every active character has the matching Crest-Dependency Authority.
- [x] An already incompatible count-five portfolio is repaired only by deterministic replacement below the threshold; otherwise automation pauses before combat.
- [x] The current 70-plus-gated Risk set and adverse Risk erosion-adjustment card are hard excluded from runtime-decoded evidence.
- [x] Conditionally eligible Risk cards require the projected erosion band and recoverability proved by the safety horizon.
- [x] Current production never infers a research-rate Code from technology or settlement data; a future authoritative selectable mechanic must supply exact family/rate before Equipment rejects it or Research applies the active-family strict-overwrite rule.
- [x] One candidate's missing family, gate, target, or effect evidence rejects only that candidate.
- [x] Tests characterize current known identifiers while proving that classification follows authoritative mechanics rather than identifier-only assumptions.
- [x] Mandatory two-axis checkpoint review passed independently (Spec PASS; Standards PASS).

**Implementation evidence:** Fresh Docker/RO inspection of `Project.dll` SHA-256 `53806a5b4dec186357e2fe8ba5b8a72e4f85674be9231479e207e500e2bd1300` and `GameAssembly.dll` SHA-256 `573fa800171b8b37800cb4425b918351ec84a340bca9a46c32249d7af965c1fb` anchors effective counts to `NetherCodeCategoryTypeExtensions.GetCategoryRawCount`/`GetCategoryCount`, opposition to Rush↔Impact and Safe↔Risk, crest identity to `ManaType` plus `BuffType.CrestPassion`/`CrestImpact`, and Risk gating to `BattleSituationAboveErosion.Percent`. Production assembles each accepted owned-Code portfolio and each candidate/removal portfolio completely; Equipment contamination repair compares those retained typed portfolios lexicographically, while Research keeps the configured active family. Conditional Risk consumes the authoritative route-horizon erosion/recoverability evidence from Ticket 03 rather than an offer-local constant. Fresh lifecycle evidence also proves a native-design deviation from the earlier research-rate assumption: `MNetherCodes` exposes only identity/category/effect parameters/asset/power, `NetherCodeModel.CreateModel` builds ability or erosion effects, `NetherResultRequestEntity` sends no Code rate, `CreateNetherResultModelAsync` consumes `nether_code_points` as server settlement outcomes, and `NetherPointData.SpherePointRatio` is separate technology. Production therefore never manufactures a current 5/10/15-percent Code classification; a future authoritative mechanic must supply exact family/rate and missing evidence remains candidate-local. Public production callers cover reversed displayed power, active Research family, opposed-family repair, Risk gates, complete-retained replacement, and candidate-local unknowns. The Docker affected suite passed 175/175 and the full suite passed 1016/1016. Two isolated RO-source/RO-game Release builds were byte-identical with 0 warnings/0 errors: `release/strategy-batch-04-06-third-review-a/Release/net6.0/AutoNether.dll` and `...-b/.../AutoNether.dll`, SHA-256 `c9f7efb19284a8f34358f9e1c158daadf911410f8586da258971bb8336c82b74`, 1,447,424 bytes. Final-review closure deletes the legacy three-argument displayed-power/coverage policy entry point; all callers and retained tests now use the authoritative evidence contract, and the forbidden overload has zero references. The affected Docker/game-RO suite passed 98/98 and the full suite passed 1018/1018. Two clean RO-source/RO-game Release builds were byte-identical at SHA-256 `966c30ca3984f8c602df5195f802c3c2b63e864a9ad0dbd3717f18b564053583`, 1,457,152 bytes, with 0 warnings/0 errors. Mandatory two-axis re-review remains pending.

**Latest review-fix evidence:** Equipment random transform is now reachable only through the real Recovery-to-Change production flow and an explicit authoritative commitment: the opt-in is enabled, Rest and Purification have exact zero actual value, and the selected held Code has a typed hard-exclusion reason. Research, default-disabled, value-bearing deterministic alternatives, missing hard-exclusion evidence, and stale/mismatched commitments all fail closed; removals use typed hard-reason priority then deterministic Code ID, never displayed power. The focused public popup/assembler/mechanism suite passed 58/58, the broader affected suite passed 221/221, and the full Docker/game-RO suite passed 1036/1036. Two builds derived from one immutable RO source snapshot and the same RO game mount were byte-identical at SHA-256 `f0fe47392d1b6b2920a6cd3dfc9310998650942b17fa399ddf47cb7e203848c4`, 1,478,144 bytes, with 0 warnings/0 errors; artifacts are `release/strategy-batch-04-06-converged2-20260816-a/Release/net6.0/AutoNether.dll` and the matching `...-b/...` build. Mandatory two-axis re-review remains pending.

**Latest lexicographic review-fix evidence:** Public production-assembler mutations now retain both candidate and removed typed Combat Tier. After every hard eligibility gate passes, a proven higher-tier mutation may trade away lower-tier value (for example Back Force Chain over front fallback or rear offense over nonessential rear defense), the reverse is rejected, and mixed same-tier or cross-domain portfolios remain Reachable-Unquantified instead of acquiring a scalar. The mutation-sensitive cross-tier RED failed 0/1 (old result `Keep`); the cross-domain staged-old RED failed 0/2 (old result `NonPositive`). The focused assembler suite passed 47/47, the affected 04–06 suite passed 231/231, and the full Docker/game-RO suite passed 1046/1046. Two clean compile-verification builds from one RO source snapshot were byte-identical at SHA-256 `c6d893fc9021a9875f06c2d2514bd0d94d77493a1fb91d27fbeb9545a5f9cee5`, 1,482,752 bytes, with 0 warnings/0 errors; final checkpoint artifact identity remains the orchestrator/reviewer responsibility after staging the immutable snapshot. Mandatory two-axis re-review remains pending.

**Comparator closure evidence:** The real production assembler now preserves a positive higher-tier qualitative mutation and a simultaneous lower-tier typed mechanism loss as separate signs until Combat Tier comparison. A Back Force Chain offer can therefore replace held front-only erosion value, while the reverse remains blocked; no unlike quantities are subtracted. The narrow mutation RED failed exactly that Select case, while the reverse already passed. After GREEN the narrow comparator suite passed 8/8, the assembler suite 55/55, the affected suite 239/239, and the full Docker/game-RO suite 1054/1054. Mandatory two-axis re-review remains pending.

**Recipient-vector closure evidence:** Under current Docker/game-RO hashes `Project.dll` `53806a5b4dec186357e2fe8ba5b8a72e4f85674be9231479e207e500e2bd1300` and `GameAssembly.dll` `573fa800171b8b37800cb4425b918351ec84a340bca9a46c32249d7af965c1fb`, opposed-family retention now compares each complete common-plus-side portfolio with the same typed recipient-aware Combat Tier vector as ordinary replacement. A common held portfolio plus Back Force Chain retains the Back Force Chain family against front-only Attack, in either Rush/Impact orientation and across incremental removals. The production RED contributed two failures to the 7-failure/12-case mutation run; GREEN passed 12/12, the assembler suite 73/73, the affected 04–06 suite 259/259, and the full Docker/game-RO suite 1074/1074. Mandatory two-axis re-review remains pending.

**Actual-marginal tier closure evidence:** Fresh `BuffController.CheckCoexistenceHigherValue` and `BuffGroup.GetSumValue`/`GetMaxValue`/`GetMaxLimit`/`GetHighestValueBuff` evidence proves that only the complete portfolio's surviving recipient/BuffType outcomes may carry strategy tier. Opposed-family comparison no longer accepts one side-wide maximum tier: a common rear HigherValue row can suppress a side rear Attack, leaving front Attack versus front Damage correctly incomparable, and a saturated rear Critical row cannot promote a surviving front Attack over a real rear-defense gain. The production RED failed all three assertions (both defense-family orientations) in a 4-failure/6-case run; GREEN passed 6/6, assembler 81/81, affected 265/265, and full 1080/1080. Mandatory two-axis re-review remains pending.

**Offer effective-tier closure evidence:** Fresh Docker/game-RO evidence under the current hashes confirms `AbilityTargetGroupBase._partyPositionFlag` is evaluated through `IsMatch`/`IsMatchFilter` per member (`CharacterPartyPositionFlag` Front=2, Back=4, Supporter=8), while `BuffController.CheckCoexistenceHigherValue` and `BuffGroup.GetHighestValueBuff` decide the surviving recipient/BuffType row. The real assembler/policy RED expected Back Defense `89903` but the old candidate-wide tier selected All Attack `89902` after its Back row was suppressed. Equipment mutation results now carry the first decisive tier from actual nonzero post-coexistence recipient/metric/mechanism/special/qualitative marginals only. Controls prove an All offer with a real Back marginal retains rear-offense tier, pure Forward remains fallback, and unknown target bits reject only that candidate. Focused controls passed 4/4, assembler 85/85, affected 269/269, and full Docker/game-RO 1084/1084; two compile-verification builds were byte-identical with 0 warnings/0 errors at SHA-256 `3bdf1e033ad24f4c4d793009626379cadc0c7e6ae270ac1d4c6c8e3caed850ae` (1,501,184 bytes). Mandatory two-axis re-review remains pending.

**Final checkpoint review closure:** Independent staged-snapshot review passed both axes: Spec PASS and Standards PASS. The reviewer materialized the exact index with `git checkout-index`, mounted the game read-only, passed the full Release suite 1084/1084, and produced two byte-identical 0-warning/0-error builds at SHA-256 `d771624e2e9fce8a90133174cc84ef5f988d1d99986bb69d6ef31834837437f2`. Fresh authorities remained `Project.dll` `53806a5b4dec186357e2fe8ba5b8a72e4f85674be9231479e207e500e2bd1300`, `GameAssembly.dll` `573fa800171b8b37800cb4425b918351ec84a340bca9a46c32249d7af965c1fb`, and metadata `ac0c6d43ca487456a5de68a5d357f634fedd9fa0a87d80d5b6545360fb133ea5`. Working-tree and staged diff checks passed; no deployment was performed.
