# 08 — Complete Research-mode progression and settlement

**What to build:** Pursue explicitly configured family wallets from floor zero, preserve projected completion, and settle normally at the next Boss without exceeding the floor-70 ceiling.

**Blocked by:** 01 — Explicit strategy modes and Boss-aligned run boundaries; 03 — Project route-horizon safety and erosion recoverability; 04 — Enforce Code-family integrity and hard eligibility; 05 — Simulate native buff timelines and parameter value; 06 — Value crest, charge, stack, erosion, and Force Chain mechanics.

**Status:** complete — implementation/review repair closure 2026-08-18; native boundary deviation is recorded in [`docs/agents/evidence-backed-strategy-modes-07-09-evidence.md`](../../../docs/agents/evidence-backed-strategy-modes-07-09-evidence.md)

- [x] Completion uses current family wallet plus projected normal Research Settlement reaching the authoritative 20,000 threshold.
- [x] Code count, Code capacity, and category-skill gauges never substitute for the wallet calculation.
- [x] Primary remains active until projected complete; secondary follows; Equipment value governs only after configured targets are complete.
- [x] While a target is incomplete, every available reroll is spent before falling back from that family, regardless of the Equipment reserve.
- [x] Hard exclusions, erosion safety, and crest compatibility override Research progress.
- [x] At capacity, replacement removes hard-excluded, opposed, ordinary non-target, then provably surplus completed-family Codes in that order.
- [x] An active-target Code is never removed for a non-target; a same-family swap preserves contribution and improves combat value.
- [x] Completion is recalculated after every acquisition and replacement and cannot regress below a previously projected threshold.
- [x] A safe active-target Reachable-Unquantified Code can count toward settlement without invented combat magnitude.
- [x] Completion before a segment end continues to the next terminal Boss and uses normal settlement.
- [x] Incomplete Research at the floor-70 Boss settles normally instead of continuing, retreating, losing intentionally, or using Lost Signal.
- [x] Acceptance tests cover primary/secondary transitions, reroll exhaustion, replacement invariants, completion regression, early completion, and floor-70 incomplete completion.

## Closure evidence and native boundary

Research policy completion is exact when an authoritative family wallet and a known projected normal
settlement are supplied. The current native client only exposes `nether_code_points` in the normal
result response and `SpherePointRatio` as technology; it does not provide a pre-settlement family
projection. Production therefore passes an unknown projection rather than inventing completion,
while the typed policy seam remains ready for a future authoritative projection. After every
primary reroll epoch is exhausted, the effective secondary target family is selected before hard
eligibility, candidate filtering, capacity removal, retained-family resolution, and strict
same-family actual-combat swaps. Typed hard exclusions override target-family protection for held
Codes; ordinary active-target Codes remain protected and completed-family invariants remain
fail-closed. The final fresh public RED/GREEN jobs are `j-9tdags`/`j-wwler8`; expanded focused
coverage passed 323/323 (`j-gmybcz`), and the full Docker suite passed 1151/1151 (`j-czkkaa`).
Fresh native hashes and anchors are recorded in the durable evidence note.

### Final gate addendum (2026-08-18)

The final gate reran the full production/test boundary after the Research effective-target repair
and verified the immutable Event projected-state handoff. Focused GREEN was `j-fqejbx` (`68/68`),
expanded focused GREEN was `j-r7q5jc` (`380/380`), and the full suite was `j-08wpm7`
(`1157/1157`). Release build `j-nhee2f` passed with `0 Warning(s), 0 Error(s)` and
product isolation `j-9xopoe` passed. Fresh native hashes and anchors remain in the linked durable
note; no native/spec conflict was found and no pre-settlement projection was invented.

### Post-repair revalidation addendum (2026-08-18)

No new Research policy change was needed after the partial-death repair. Fresh RO Cpp2IL job
`j-hvd4j4` returned `CPP2IL_EXIT=0` and `DIFFABLE_EXIT=0` against unchanged native inputs:
`Project.dll` `53806a5b4dec186357e2fe8ba5b8a72e4f85674be9231479e207e500e2bd1300`,
`GameAssembly.dll` `573fa800171b8b37800cb4425b918351ec84a340bca9a46c32249d7af965c1fb`, and
`global-metadata.dat` `ac0c6d43ca487456a5de68a5d357f634fedd9fa0a87d80d5b6545360fb133ea5`.
Its authoritative anchors at `j-hvd4j4/output.log:405-424` show native `current_hp_ratio`,
response character/floor/Code rows, and the floor/index/selected-option/Code request; they do not
provide a hidden Research projection or any inferred family rule.

The Research secondary-target RED/GREEN evidence remains `j-9tdags` to `j-wwler8`. Current clean
revalidation passed focused `j-rh669t` (`137/137`), expanded `j-ehq5wo` (`313/313`), full
`j-f5znai` (`1166/1166`), and Release `j-mets3c` (`0 Warning(s), 0 Error(s)`). Product-isolation
passed in `j-x71jbk`; every validation command used `docker run --rm` and a read-only game mount.
### ResearchRateOverwrite repair addendum (2026-08-18)

The typed ResearchRateOverwrite seam is now explicitly copied by
NetherCodePolicyEvidenceAssembler.MapHardEligibility. A separately authoritative typed Known
value is preserved; current production native capture explicitly records NotPresent because fresh
native MNetherCodes.cs has only effect_type, effect_parameter_1..3, asset_id, and power
(j-bbli4l/output.log:46-71, artifact hash
f321ea53eeb23c9130f8fa4b090c7c9b6f574cca6bbfa9eb8a21dec46c0046f5). Fresh
NetherResultResponseEntity.cs and NetherPointData.cs output (j-bbli4l/output.log:82-126) provides
no selectable research-rate mechanic, so unknown remains fail-closed without a native/spec
deviation. The RED was j-pvy9dv (typed value 15 observed as 0); GREEN was j-m4o95q (5/5,
including known typed propagation and candidate-local unknown behavior). Final focused/expanded/full
Docker validation passed 115/115, 293/293, and 1173/1173, and Release passed with 0 warnings and
0 errors.

### Current closing repair addendum (2026-08-18)

The closing cycle retained the typed ResearchRateOverwrite seam as fail-closed when no native
selectable mechanic is proven, and kept raw battle tiers unknown. Fresh native RO job `j-53m4lb`
(`CPP2IL_EXIT=0`, `DIFFABLE_EXIT=0`) records the native boundaries in the durable evidence note.
Focused GREEN `j-voopjz` passed 196/196, including Research and typed evidence tests; no Research
rule was broadened beyond authoritative evidence.

### Closing P1 repair addendum (2026-08-18)

No Research target-family or completion rule changed. The shared production route-owned procurement
producer remains exact and durable across repeated route-safety capture, while ordinary Event HP
reconciliation now requires every living character's exact projected HP; authorized partial-death
exceptions remain limited to their typed commitment. Focused GREEN `j-n05h3m` passed 53/53, expanded
focused `j-ulroy1` passed 313/313, full `j-6h5nv7` passed 1183/1183, Release `j-qhiwpe` passed with
0 warnings and 0 errors, and product isolation `j-anvi44` passed. Fresh native evidence and hashes
are in the durable note; no hidden Research mechanic was inferred.

### Documentation integrity addendum (2026-08-18)

Historical pre-amend record: this ticket was part of task-group commit `1e1e7a0d6f0215910e9b7d1254c7771d217326ea`, whose
parent is `5f3de38572d5526e73e8576ffe505669c1c8dbc3`. The exact recorded closing Docker gate is
the current set `j-l9m0j6` (focused 111/111), `j-529vvn` (expanded 230/230), `j-nyu1bj`
(full 1186/1186), `j-3ulak2` (Release 0 warnings/0 errors), and `j-ju3ij7`
(`PRODUCT_ISOLATION_PASS=1`, `DIFF_CHECK_EXIT=0`); `/game` was mounted read-only in each.
The exact pre-amend HEAD and parent are recorded in `j-schqct/output.log:116-117`.

### Current canonical-restore closing record (2026-08-18)

Clean restore RED `j-urwq7m/output.log:1-6` reproduced NU1101 for both BepInEx packages;
clean restore GREEN `j-g4xeyq/output.log:1-9` used repository `NuGet.config` without source
flags and passed 1184/1184. Fresh RO native Cpp2IL `j-53m4lb/output.log:1-150` returned
`CPP2IL_EXIT=0` and `DIFFABLE_EXIT=0`; its immutable hashes and raw native anchors are
recorded in the durable evidence note. The historical pre-amend pin (not final) was
`1e1e7a0d6f0215910e9b7d1254c7771d217326ea`, parent
`5f3de38572d5526e73e8576ffe505669c1c8dbc3`.

### Closing native ItemType overflow repair (2026-08-18; pre-amend)

No Research policy or reroll behavior changed. This shared production evidence repair starts from
`ffa3ef96ba7862456e668195fdea6207b69543a5`, parent
`5f3de38572d5526e73e8576ffe505669c1c8dbc3`. Fresh native RO jobs `j-bghfub` and `j-5l2ncz`
returned Cpp2IL/DIFFABLE exit 0; `MItems.cs` hash
`e69e8310aa256e60e356e84e857e1b7f92f056a952c03b96f9182e865cfd0d27`, source line 11, proves
raw `long type`, and no narrower native item-type domain was proven. The same runs recorded
Project/GameAssembly/metadata hashes
`53806a5b4dec186357e2fe8ba5b8a72e4f85674be9231479e207e500e2bd1300`,
`573fa800171b8b37800cb4425b918351ec84a340bca9a46c32249d7af965c1fb`, and
`ac0c6d43ca487456a5de68a5d357f634fedd9fa0a87d80d5b6545360fb133ea5`.

The public RED `j-lzlkcg` failed `1/1` with the reproduced overflow; GREEN `j-ofgd9w` passed
`1/1`. Final focused/expanded/full Docker gates passed `207/207`, `477/477`, and `1209/1209`
(`j-8dph58`, `j-ie7n3h`, `j-1y9bfs`); Release 0/0, product isolation, and diff check passed
in `j-gwj93w`. This is a shared fail-closed evidence seam only; no Research semantic inference
was added. The post-amend Docker audit and Git note carry the final commit identity.

### Procurement source and snapshot invalidation repair (2026-08-18; pre-amend)

The shared route-owned producer now compares the previous authoritative snapshot before merging
Research/Equipment pending state, so a replan cannot inherit an old Gold/Key objective. The
selected-path producer accepts only the repository-proven Gold thresholds 200/300/500 and requires
the exact rank-five equipment-bag `ItemType=91` before a key budget. RED `j-xyz67j` was 3 failed /
0 passed / 3 total; GREEN `j-3l24iu` was 3/3. Focused `j-4gqmbu` passed 123/123, expanded
`j-u32ye3` passed 189/189, full `j-eg7dic` passed 1208/1208, and Release `j-hrbptk` passed
0 warnings/0 errors. Fresh RO native decomp `j-p18rn7` returned CPP2IL/DIFFABLE exit 0 and
proves raw Event/part/battle fields plus the floor/index/option/Code update seam; `j-phv0bh`
additionally anchors the raw native shop currency field. The exact immutable hashes and anchors
are recorded in `docs/agents/evidence-backed-strategy-modes-07-09-evidence.md`. Pre-amend HEAD is
`b837c5ce1822b3b05990ff34df62ad75a974877e`, parent `5f3de38572d5526e73e8576ffe505669c1c8dbc3`;
the post-amend identity is authoritative only in the final Git note.

### Same-branch route identity and authoritative invalidation repair (2026-08-18; pre-amend)

Research shares the route-owned procurement producer with Equipment, and it now receives only the
selected horizon `Steps` for branch-local commitment generation. `BeginRouteReplan` clears prior
proof and every authoritative snapshot confirmation invalidates a changed snapshot before a new
Research/Event plan. The public RED failed both new route-coordinator tests (2/2) before the fix;
GREEN `j-0t56g0` passed 4/4, including the bridge invalidation seam and production alternate-branch
controller E2E. Fresh native RO Cpp2IL `j-ard4dj` passed with immutable game/decomp hashes and
raw Event/Part/battle/native-update anchors recorded in the durable evidence note; native evidence
does not expose a hidden route-budget or alternate-branch semantic and no such behavior was
inferred. Expanded `j-e6tp0m` passed 120/120, full `j-7lbau3` passed 1205/1205, Release
`j-bt997x` passed 0 warnings/0 errors, isolation `j-xzpkyj` passed, and diff `j-xzpkyj` passed.
The pre-amend tree is `d38ed66c7bd247129266d01b53e0daa85f3a90a1` with parent
`5f3de38572d5526e73e8576ffe505669c1c8dbc3`; the post-amend exact identity will be in
`refs/notes/logic-overhaul-evidence`.

### Current final-repair audit (2026-08-18; supersedes prior pins)

Prior SHA/count paragraphs above are historical pre-amend records and are not the final task-group
identity. The current implementation tree before the in-place amend is
`beb8824604298da985965bad332b24ac9d7845c7`, parent
`5f3de38572d5526e73e8576ffe505669c1c8dbc3`. Because this ticket is included in the amended tree,
the final commit's own SHA cannot be embedded without changing that SHA; the post-amend Docker
audit prints the actual final `HEAD`, `HEAD^`, tree, and status and is authoritative.

Fresh native RO Cpp2IL is `j-cg7xis/output.log:1-161` (`CPP2IL_EXIT=0`, `DIFFABLE_EXIT=0`), with
the immutable game hashes and raw Event/battle/update anchors recorded in the durable note. The
native output proves no selectable ResearchRateOverwrite mechanic and no battle semantic mapping
for raw battle type alone; the typed seam therefore remains fail-closed. Fresh REDs were
`j-x4qiyb` (3 failed / 1 passed / 4 rarity tests) and `j-xlvwrk` (2 failed / 0 passed / 2
route-producer tests); GREEN was `j-b6bdes` (128/128), expanded `j-g23akv` (337/337), full
`j-n7g4ih` (1201/1201), Release `j-dbw8t4` (0 warnings/0 errors), and isolation/diff
`j-1lixdk` (PASS/0). Every command used `docker run --rm` with `/game` read-only.

### Final-gate route-owned procurement repair (2026-08-18)

No Research family, reroll, or completion rule changed. The production route plan now carries only
proven option-projection procurement commitments, while the real runtime bridge preserves exact
Gold/Key minima over repeated empty captures. RED `j-vii6om` reproduced the empty real-bridge
capture (1 failed / 0 passed / 1 total); GREEN `j-dyqez9` passed the real production bridge and
option-local unsafe-spend E2E (1/1), expanded `j-am5nzl` passed 109/109, and full `j-vtk92z`
passed 1184/1184. Release `j-xw2sb3` was 0 warnings/0 errors and product isolation `j-6r4fg3`
passed. Fresh native RO decomp `j-9lqbic` returned CPP2IL/DIFFABLE exit 0; its immutable game
hashes and raw Event/battle/update anchors are recorded in the durable evidence note. Current
native evidence still proves no selectable ResearchRateOverwrite mechanic, so that typed seam
remains fail-closed. The historical evidence pin (not final) was `1e1e7a0d6f0215910e9b7d1254c7771d217326ea`
with parent `5f3de38572d5526e73e8576ffe505669c1c8dbc3`.

### Current procurement-priority validation (2026-08-18)

The shared Event policy repair is mode-safe: committed Gold/key threshold priority is applied
only from exact option-local route commitments and does not alter Research family or settlement
rules. The Equipment production RED `j-kmf1l2` failed 2/2 threshold tests and GREEN `j-1ro4y6`
passed 2/2. Fresh native RO evidence is `j-86iu89/output.log:1-150`; no hidden Research or
procurement mechanic was inferred. Current focused/expanded/full results are 111/111, 230/230,
and 1186/1186 (`j-l9m0j6`, `j-529vvn`, `j-nyu1bj`), Release `j-3ulak2` is 0/0, and audit
`j-ju3ij7` passes with `/game` read-only. Historical pre-amend pin (not final):
`1e1e7a0d6f0215910e9b7d1254c7771d217326ea`, parent
`5f3de38572d5526e73e8576ffe505669c1c8dbc3`.
