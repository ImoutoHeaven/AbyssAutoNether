# Domain Documentation

This repository has one bounded context for Abyss AutoNether strategy and automation.

- The canonical glossary is the root [`CONTEXT.md`](../../CONTEXT.md).
- Context-wide architectural decisions, when one meets the ADR threshold, belong under `docs/adr/`.
- Do not create a `CONTEXT-MAP.md` unless the repository is later split into genuinely separate bounded contexts.
